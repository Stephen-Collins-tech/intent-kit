dataset:
  name: "classifier_node_llm"
  description: "Test DAG ClassifierNode with intent classification labels"
  node_type: "classifier"
  node_name: "ClassifierNode"

test_cases:
  - input: "What's the weather like in New York?"
    expected: "weather"
    context:
      user_id: "user123"

  - input: "How's the temperature in London?"
    expected: "weather"
    context:
      user_id: "user123"

  - input: "Can you tell me the weather forecast for Tokyo?"
    expected: "weather"
    context:
      user_id: "user123"

  - input: "What's the weather like today?"
    expected: "weather"
    context:
      user_id: "user123"

  - input: "I need to cancel my flight reservation"
    expected: "cancel"
    context:
      user_id: "user123"

  - input: "Cancel my hotel booking"
    expected: "cancel"
    context:
      user_id: "user123"

  - input: "I want to cancel my restaurant reservation"
    expected: "cancel"
    context:
      user_id: "user123"

  - input: "Please cancel my appointment"
    expected: "cancel"
    context:
      user_id: "user123"

  - input: "Cancel my subscription"
    expected: "cancel"
    context:
      user_id: "user123"

  - input: "I need to cancel my order"
    expected: "cancel"
    context:
      user_id: "user123"
